---
description: Repository-wide workflow rules for Cursor agent in dimos
globs:
 - "**/*"
alwaysApply: true
---

# Cursor 在本仓库的硬性行为约束

> 这是 alwaysApply 规则，优先级高于 chat 里的临时指令。
> 若用户请求与本规则冲突，先暂停并说明冲突点，等待明确放行。

## 必须做

1. 任何改动前先读 `AGENTS.md`，确认范围和风险分级。
2. 任何改动后至少执行一次 `bash scripts/verify.sh`，失败就进入修复循环。
3. 代码必须通过 PR 合并到 `main`，禁止直推。
4. 需要 GitHub 网页人工操作时，输出明确待办清单并等待用户确认。
5. 每完成一个阶段都要汇报并等待用户确认后再继续。

## 严格禁止

1. 禁止 `git push origin main` 直推主分支。
2. 禁止 `git push --force` 到任何协作分支。
3. 禁止提交 secrets（`.env`、token、私钥、密码、凭据）。
4. 禁止通过删测试、弱化校验、篡改退出码来制造“假绿”。
5. 禁止无用户授权的大规模重构、批量改名、目录迁移。

## 工具与流程约束

- 文件读写优先用 IDE 工具，不用 `cat/head/tail/grep` 等替代。
- GitHub 操作优先用 `gh` CLI；SSH remote 必须用 `git@github.com:OWNER/REPO.git`。
- CI 与本地验证必须统一到 `bash scripts/verify.sh`。

## 完成输出要求

每次任务收尾至少包含：
1. 改动文件清单；
2. 执行过的验证命令及结果；
3. PR 链接（若已创建）；
4. 仍需人工完成的事项。
